import { DashRemoverPipe } from './dash-remover.pipe';

describe('DashRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new DashRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
