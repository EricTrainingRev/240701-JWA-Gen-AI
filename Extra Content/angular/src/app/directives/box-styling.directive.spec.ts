import { BoxStylingDirective } from './box-styling.directive';

describe('BoxStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxStylingDirective();
    expect(directive).toBeTruthy();
  });
});
